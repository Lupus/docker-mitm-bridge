apiVersion: v1
kind: Service
metadata:
  name: xds-service
  namespace: kyverno-intercept
  labels:
    app: xds-service
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: xds-service